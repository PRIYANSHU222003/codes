#include<stdio.h>
int main()
{
int arr[3][3],i,j,sum=0;
printf("enter matrix ");
for(i=0,i<3,i++)
{
for(j=0,j<3,j++)
{
scanf("%d",arr[i][j]);
}
}
for(i=0,j=2,i<3,j--,i++)
{
sum+=arr[i][j];
}
printf("sum = %d",sum);
return 0;
}
