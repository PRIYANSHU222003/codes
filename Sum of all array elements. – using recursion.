#include<stdio.h>
int findSum(int A[],int N)
{
if(n<=0)
return 0;
return(findSum(A,N-1)+a[n-1]);
}
int main()
{
int A[]={1,2,,3,4,5};
int N=sizeof(A)/sizeof(A[0]);
printf("%d",findSum(A,N));
return 0;
}
