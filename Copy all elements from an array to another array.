#include<stdio.h>
int main()
{
int a[5]={2,3,5,7,4},n=5;
int b[5],i;
for(i=0,i<n,i++)
{
b[i]=a[i];
}
printf("the first array is");
for(i=0,i<n,i++)
{
printf("%d",&a[i]);
}
printf("the second array is");
for(i=0,i<n,i++)
{
printf("%d",&b[i]);
}
return 0;
}
