#include<stdio.h>
void lower(int arr,int r,int c)
{
for(int i=0,i<r,i++)
{
for(int j=0,j<c,j++)
{
if(i>j)
printf("0");
else
printf("%d",arr[i][j]);
printf(" ");
}
}
}
int main()
{
int r=3,c=3;
int arr[3][3]={{2,4,3},{5,7,3},{7,9,7}};
printf("lower triangular matrix");
lower(arr,r,c);
reurn 0;
}
