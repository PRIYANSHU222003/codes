#include<stdio.h>
voit upper(int arr[3][3],int r,int c)
{
for(int i=0,i<r,i++)
{
for(int j=0,j<c,j++)
{
if(i<j)
printf("0");
else
printf("%d",a[i][j]);
printf(" ");
}
]
}
int main()
{
int r=3,c=3;
  int arr[3][3]={{1,2,3},{4,5,6},{7,8,9}};
printf("upper triangular matrix");
upper(arr,r,c);
return 0;
}
